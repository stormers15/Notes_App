name: notes_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you plan to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  modal_progress_hud_nsn: ^0.5.0 
  flutter_bloc: ^8.1.6
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  font_awesome_flutter: ^9.0.0
  hive: ^2.2.3 
  hive_flutter: ^1.1.0 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.3.2
  hive_generator: ^2.0.0  # Move hive_generator to dev_dependencies

flutter:
  uses-material-design: true

  fonts:
    - family: Poppins
      fonts:
        - asset: assets/fonts/Poppins-Regular.ttf

# Remove hive_generator configuration from here
